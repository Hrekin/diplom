= form_for @role do |f|
  - if @role.errors.any?
    #error_explanation
      %h2= "#{pluralize(@role.errors.count, "error")} prohibited this role from being saved:"
      %ul
        - @role.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :role_name
    = f.text_field :role_name
  .field
    = f.label :role_discription
    = f.text_field :role_discription
  .actions
    = f.submit 'Save'
